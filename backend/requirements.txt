fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
pydantic>=2.7
# EmailStr support for request validation (pydantic[email]).
email-validator>=2.0
pydantic-settings>=2.3
# Multipart form parsing — required by FastAPI's UploadFile (the logo upload).
python-multipart>=0.0.9
PyJWT>=2.8
sqlmodel>=0.0.14
bcrypt>=4.0
# Request rate limiting (login + contact endpoints); keyed on client IP.
slowapi>=0.1.9
# Async HTTP client — runtime dep for the Telegram Bot API integration (app/telegram).
httpx>=0.27
# Postgres driver (v3). SQLite needs no extra driver; used when DATABASE_URL is postgresql://
psycopg[binary]>=3.1
# Image processing for the logo upload: uploads are decoded, downscaled and re-encoded
# to WebP before they touch the disk. That (a) keeps a 5 MB phone photo from being
# stored at 5 MB, (b) strips EXIF/GPS metadata, and (c) lets us reject decompression
# bombs by inspecting the header's pixel count before decoding.
Pillow>=10.0
