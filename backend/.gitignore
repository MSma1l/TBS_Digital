__pycache__/
*.py[cod]
.venv/
venv/
.env
# keep the template (the root .gitignore's .env* rule would otherwise drop it)
!.env.example
.pytest_cache/
# Runtime persistence (not source): JSON stand-in files and the SQLite DB
data/*.json
data/*.db
*.sqlite
*.sqlite3
