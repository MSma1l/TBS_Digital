// The Twitter/X card reuses the exact same branded image as Open Graph — one source of
// truth, served at /twitter-image so the twitter:image tag is emitted alongside og:image.
export { default, alt, size, contentType } from "./opengraph-image";
